let express,readdirSync,cors,mongoose;_5fe‍.w("express",[["default",["express"],function(v){express=v}]]);_5fe‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_5fe‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_5fe‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]); 



const morgan = require("morgan");
require('dotenv').config(); 

const app = express();

// db connection 
mongoose
  .connect(process.env.DATABASE, {
      useNewUrlParser: true,
      useFindAndModify: false, 
      useUnifiedTopology: true,
      useCreateIndex: true,
  })
  .then(() => console.log("DB connected"))
  .catch((err) => _5fe‍.g.console.log("DB Error => ", err));

// middlewares 
app.use(cors());
app.use(morgan("dev")); 
app.use(express.json());

// route middleware 
readdirSync("./routes").map((r) => app.use('/api', require(`./routes/${r}`))); 

const port = process.env.PORT || 8000;

app.listen(8000, () => console.log(`Server is running on port ${port}`));